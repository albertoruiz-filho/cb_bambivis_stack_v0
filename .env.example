# Database Configuration
POSTGRES_DB=cb_bambivis
POSTGRES_USER=cbuser
POSTGRES_PASSWORD=change_this_password_in_production

# Backend Configuration
NODE_ENV=production
API_PORT=3000
JWT_SECRET=change_this_secret_in_production

# Frontend Configuration
FRONTEND_PORT=3001
REACT_APP_API_URL=http://localhost:3000

# Nginx Configuration (for production)
NGINX_PORT=80

# Docker Images (optional - use custom images if available)
# BACKEND_IMAGE=your-registry/cb-bambivis-backend:latest
# FRONTEND_IMAGE=your-registry/cb-bambivis-frontend:latest
